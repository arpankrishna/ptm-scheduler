import React from 'react'
import ReactDOM from 'react-dom/client'
import PTMScheduler from './PTMScheduler'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <PTMScheduler />
  </React.StrictMode>,
)
